function lowercase (string) {

    return string.toLowerCase()
}


console.log(lowercase("HEJ"))


forEach (taskData) {
    const {id, title,date, description} = ""
    
  }


taskData.forEach(task) {

}



const sortedValues = inputValues.sort((a, b) => {
  for(i = 0; i <inputValues.length; i++) {
    if(array[i] < 0 ) {
      inputValues.sort(a);
    } else if( array[i] > 0){
      inputValues.sort(b)
    } else {
      return a-b;
    }
  }
});